torch>=1.6.0
torchvision>=0.7.0
numpy>=1.19.0
tqdm>=4.50.0
opencv-python>=4.4.0
plyfile>=0.7.4
matplotlib>=3.3.0
# open3d>=0.15.1  # 注释掉，因为我们现在使用matplotlib进行可视化
# pip install --upgrade-strategy only-if-needed --no-cache-dir -r requirements.txt