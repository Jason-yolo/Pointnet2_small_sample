# 环境依赖检查工具使用说明

## 工具介绍

`check_env.py` 是一个用于检查Python环境依赖包是否正确安装的工具。该工具支持从requirements.txt文件读取依赖列表或使用内置的默认依赖列表，能够检查每个依赖的安装状态和版本是否满足要求，并提供清晰的检查结果和安装建议。

## 主要功能

- ✅ 支持从requirements.txt文件读取依赖列表
- ✅ 内置PointNet2项目的默认依赖列表
- ✅ 检查依赖包的安装状态和版本兼容性
- ✅ 提供彩色输出，使结果清晰易读
- ✅ 为缺失或版本不匹配的依赖生成安装建议
- ✅ 支持一键安装所有缺失的依赖
- ✅ 跨平台兼容（Windows和Linux）
- ✅ 良好的错误处理机制

## 使用方法

### 基本用法

在项目根目录下运行以下命令使用默认依赖列表（基于PointNet2项目）进行检查：

```bash
python check_env.py
```

### 指定requirements.txt文件

```bash
python check_env.py -r requirements.txt
# 或者
python check_env.py --requirements /path/to/your/requirements.txt
```

### 禁用彩色输出

如果在某些环境中彩色输出不正常，可以禁用彩色输出：

```bash
python check_env.py --no-color
```

## 输出说明

工具会输出三种类型的依赖信息：

1. **已成功安装的依赖**（绿色）：显示包名、已安装版本和版本要求
2. **版本不匹配的依赖**（黄色）：显示包名、已安装版本和需要的版本
3. **未安装的依赖**（红色）：显示包名和版本要求

最后，工具会为所有缺失或版本不匹配的依赖生成安装命令，包括单个安装命令和一键安装所有依赖的命令。

## 错误处理

工具具有完善的错误处理机制，可以处理以下情况：

- requirements.txt文件不存在或无法读取
- 版本解析错误
- 包检查过程中的异常
- 用户中断操作

## 跨平台兼容性

- **Windows**：自动处理控制台颜色支持
- **Linux/Mac**：原生支持彩色输出
- 安装命令在所有平台上通用

## 关于默认依赖列表

工具内置了PointNet2项目的基本依赖列表：
- torch>=1.6.0
- torchvision>=0.7.0
- numpy>=1.19.0
- tqdm>=4.50.0
- opencv-python>=4.4.0
- plyfile>=0.7.4

## 作为脚本集成

该工具也可以作为Python模块集成到其他脚本中使用：

```python
from check_env import DependencyChecker

checker = DependencyChecker()
satisfied, version_mismatch, missing = checker.check_all_dependencies(['numpy>=1.19.0', 'torch>=1.6.0'])
checker.print_results(satisfied, version_mismatch, missing)
```

## 注意事项

1. 请确保在正确的Python环境中运行该工具，它将检查当前环境中的依赖
2. 对于某些需要编译的包（如torch），安装可能需要额外的系统依赖
3. 如果使用虚拟环境，请确保已激活虚拟环境后再运行该工具