# PointNet2项目依赖库推荐

## 一、基础依赖（已在requirements.txt中）

| 依赖库 | 版本要求 | 作用 | 应用场景 |
|--------|----------|------|----------|
| torch | >=1.6.0 | 深度学习框架 | 所有机器学习任务 |
| torchvision | >=0.7.0 | 计算机视觉工具包 | 图像处理、数据加载 |
| numpy | >=1.19.0 | 数值计算库 | 数据处理、矩阵运算 |
| tqdm | >=4.50.0 | 进度条显示 | 训练过程可视化 |
| opencv-python | >=4.4.0 | 计算机视觉库 | 点云可视化、图像处理 |
| plyfile | >=0.7.4 | PLY文件读写 | 点云文件格式处理 |

## 二、扩展依赖推荐

### 1. 点云处理与分析

| 依赖库 | 推荐版本 | 作用 | 应用场景 | 兼容性说明 |
|--------|----------|------|----------|------------|
| open3d | >=0.15.0 | 3D数据处理库 | 点云滤波、配准、分割 | 与PyTorch 1.6+兼容 |
| pclpy | >=0.13.0 | PCL的Python绑定 | 高级点云处理算法 | 需要对应版本的PCL库 |
| laspy | >=2.0.0 | LAS/LAZ文件处理 | 激光雷达数据处理 | Python 3.6+ |
| trimesh | >=3.9.0 | 3D网格处理 | 网格生成、操作 | 轻量级，易于集成 |

### 2. 高级可视化

| 依赖库 | 推荐版本 | 作用 | 应用场景 | 兼容性说明 |
|--------|----------|------|----------|------------|
| matplotlib | >=3.3.0 | 绘图库 | 结果可视化、图表生成 | 与numpy兼容 |
| mayavi | >=4.7.4 | 3D科学可视化 | 高级点云渲染 | 需要VTK支持 |
| plotly | >=5.0.0 | 交互式可视化 | 交互式点云展示 | 网页友好，支持导出 |
| pyvista | >=0.34.0 | 3D可视化与分析 | 高质量点云渲染 | 基于VTK，API友好 |

### 3. 数据增强与预处理

| 依赖库 | 推荐版本 | 作用 | 应用场景 | 兼容性说明 |
|--------|----------|------|----------|------------|
| scikit-learn | >=0.24.0 | 机器学习工具 | 数据预处理、特征提取 | 与numpy、scipy配合使用 |
| scipy | >=1.6.0 | 科学计算库 | 高级数学运算、信号处理 | 与numpy兼容 |
| albumentations | >=1.0.0 | 数据增强库 | 点云/图像增强 | 主要针对2D，3D需扩展 |
| imgaug | >=0.4.0 | 图像增强库 | 点云投影图像增强 | Python 3.6+ |

### 4. 模型评估与分析

| 依赖库 | 推荐版本 | 作用 | 应用场景 | 兼容性说明 |
|--------|----------|------|----------|------------|
| pandas | >=1.2.0 | 数据分析库 | 实验结果分析、报表生成 | 与numpy协同工作 |
| seaborn | >=0.11.0 | 统计可视化 | 结果可视化、热图生成 | 基于matplotlib |
| tensorboard | >=2.5.0 | 模型可视化工具 | 训练监控、性能分析 | 可与PyTorch配合使用 |
| weightandbiases | >=0.12.0 | 实验跟踪平台 | 实验管理、结果共享 | 需要账号，支持免费版 |

### 5. 开发与部署工具

| 依赖库 | 推荐版本 | 作用 | 应用场景 | 兼容性说明 |
|--------|----------|------|----------|------------|
| pytest | >=6.2.0 | 测试框架 | 代码测试、质量保证 | Python标准测试工具 |
| black | >=21.0 | 代码格式化 | 代码风格统一 | 遵循PEP 8 |
| flake8 | >=3.9.0 | 代码检查 | 代码质量检查 | 静态代码分析 |
| onnx | >=1.9.0 | 模型交换格式 | 模型导出、部署 | 与PyTorch 1.6+兼容 |
| onnxruntime | >=1.8.0 | ONNX推理引擎 | 模型部署、推理优化 | 支持CPU/GPU推理 |

## 三、不同应用场景的完整依赖方案

### 1. 基础点云分类/分割

```bash
pip install numpy>=1.19.0 torch>=1.6.0 torchvision>=0.7.0 opencv-python>=4.4.0 plyfile>=0.7.4 tqdm>=4.50.0
```

### 2. 高级点云处理与分析

```bash
pip install numpy>=1.19.0 torch>=1.6.0 torchvision>=0.7.0 opencv-python>=4.4.0 plyfile>=0.7.4 tqdm>=4.50.0 open3d>=0.15.0 scikit-learn>=0.24.0 scipy>=1.6.0 matplotlib>=3.3.0
```

### 3. 点云可视化与展示

```bash
pip install numpy>=1.19.0 opencv-python>=4.4.0 plyfile>=0.7.4 matplotlib>=3.3.0 mayavi>=4.7.4 pyvista>=0.34.0 plotly>=5.0.0
```

### 4. 完整研究环境

```bash
pip install numpy>=1.19.0 torch>=1.6.0 torchvision>=0.7.0 opencv-python>=4.4.0 plyfile>=0.7.4 tqdm>=4.50.0 open3d>=0.15.0 scikit-learn>=0.24.0 scipy>=1.6.0 matplotlib>=3.3.0 pandas>=1.2.0 seaborn>=0.11.0 tensorboard>=2.5.0 pytest>=6.2.0 black>=21.0
```

### 5. 生产部署环境

```bash
pip install numpy>=1.19.0 torch>=1.6.0 torchvision>=0.7.0 opencv-python>=4.4.0 plyfile>=0.7.4 onnx>=1.9.0 onnxruntime>=1.8.0
```

## 四、版本兼容性重要说明

1. **PyTorch与CUDA版本匹配**：
   - PyTorch 1.6.0 兼容 CUDA 10.1/10.2/11.0
   - PyTorch 1.7.0+ 兼容 CUDA 11.0/11.1/11.2
   - 建议：使用官方推荐的CUDA版本安装对应PyTorch

2. **Python版本兼容性**：
   - 所有依赖库推荐在Python 3.6+环境下运行
   - 建议使用Python 3.8或3.9以获得最佳兼容性

3. **NumPy版本注意事项**：
   - NumPy 1.19.0+与PyTorch 1.6.0+完全兼容
   - 避免使用过低版本NumPy，可能导致内存管理问题

4. **OpenCV版本选择**：
   - opencv-python>=4.4.0 稳定且兼容性好
   - 如果只需基础功能，可使用 opencv-python-headless 减少依赖