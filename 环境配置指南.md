# PointNet2 项目环境配置指南

## 环境配置脚本说明

本项目提供了跨平台的环境配置脚本，支持Windows和Linux/Mac操作系统，能够自动安装所有必要的依赖包并配置运行环境。

### 提供的配置脚本

1. **`setup_env.ps1`** - Windows PowerShell 脚本
2. **`setup_env.sh`** - Linux/Mac Bash 脚本

## Windows 环境配置步骤

1. **打开Windows PowerShell**
   - 以管理员身份运行PowerShell（右键点击PowerShell图标，选择"以管理员身份运行"）

2. **运行配置脚本**
   ```powershell
   # 在项目根目录下执行
   .\setup_env.ps1
   ```

3. **注意事项**
   - 如果出现执行策略错误，请先运行：`Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process`
   - 脚本会自动创建虚拟环境、安装依赖并验证配置

## Linux/Mac 环境配置步骤

1. **打开终端**

2. **赋予脚本执行权限**
   ```bash
   chmod +x setup_env.sh
   ```

3. **运行配置脚本**
   ```bash
   # 在项目根目录下执行
   ./setup_env.sh
   ```

## 脚本功能说明

### 自动执行的操作

1. **环境检查**
   - 检查Python是否已安装及版本
   - 验证操作系统兼容性

2. **虚拟环境管理**
   - 创建名为`pointnet2_env`的虚拟环境
   - 自动激活虚拟环境

3. **依赖安装**
   - 升级pip至最新版本
   - 从requirements.txt安装项目依赖：
     - torch>=1.6.0
     - torchvision>=0.7.0
     - numpy>=1.19.0
     - tqdm>=4.50.0
     - opencv-python>=4.4.0
     - plyfile>=0.7.4

4. **CUDA支持检测**
   - 自动检测CUDA是否可用
   - 如果未检测到CUDA，提供警告信息

5. **目录结构初始化**
   - 创建必要的日志目录：`log/classification`

6. **安装验证**
   - 检查所有必要的包是否成功安装

### 执行结果反馈

- **绿色输出**: 表示操作成功或验证通过
- **黄色输出**: 表示提示信息或正在进行的操作
- **红色输出**: 表示错误信息，需要用户注意

## 环境使用说明

### 激活虚拟环境

**Windows:**
```powershell
.\pointnet2_env\Scripts\Activate.ps1
```

**Linux/Mac:**
```bash
source pointnet2_env/bin/activate
```

### 运行项目示例

**训练分类模型:**
```bash
# 使用PointNet++单尺度分组模型
python train_cls.py --model pointnet2_cls_ssg --batch_size 16

# 使用PointNet++多尺度分组模型
python train_cls.py --model pointnet2_cls_msg --batch_size 16
```

**测试分类模型:**
```bash
# 使用PointNet++单尺度分组模型测试
python test_cls.py --model pointnet2_cls_ssg --batch_size 16
```

## 常见问题解决

### 1. CUDA相关问题
- **问题**: CUDA内存不足
  **解决**: 减小batch_size参数，例如`--batch_size 8`

### 2. 依赖安装问题
- **问题**: PyTorch安装失败
  **解决**: 访问 [PyTorch官网](https://pytorch.org/) 获取适合您CUDA版本的安装命令

### 3. 虚拟环境问题
- **问题**: 无法激活虚拟环境
  **解决**: 确保路径正确，或者手动创建新的虚拟环境

## 自定义环境配置

如果需要自定义环境配置，可以修改以下参数：

- **虚拟环境名称**: 修改脚本中的`pointnet2_env`为您想要的名称
- **依赖版本**: 编辑`requirements.txt`文件中的版本号
- **CUDA版本**: 可以在安装PyTorch时指定特定的CUDA版本

## 总结

本环境配置脚本提供了完整的自动化环境设置流程，能够在Windows和Linux/Mac系统上快速配置PointNet2项目所需的运行环境。脚本包含详细的执行结果反馈，方便用户了解配置进度和可能出现的问题。