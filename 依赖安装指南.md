# PointNet2项目依赖安装指南

## 一、安装前准备

### 1. 虚拟环境创建

强烈建议在虚拟环境中安装所有依赖，避免系统环境污染：

**Windows环境**：
```powershell
# 使用venv
python -m venv pointnet_env
pointnet_env\Scripts\activate

# 或使用conda
conda create -n pointnet_env python=3.8
conda activate pointnet_env
```

**Linux/Mac环境**：
```bash
# 使用venv
python3 -m venv pointnet_env
source pointnet_env/bin/activate

# 或使用conda
conda create -n pointnet_env python=3.8
conda activate pointnet_env
```

### 2. CUDA环境检查

在安装PyTorch前，确认系统CUDA版本：

```bash
# Windows/Linux
nvcc --version

# 或检查NVIDIA驱动
nvidia-smi
```

## 二、核心依赖安装

### 1. 基础依赖安装

使用项目提供的requirements.txt：

```bash
pip install -r requirements.txt
```

### 2. PyTorch安装（与CUDA匹配）

根据CUDA版本选择合适的PyTorch安装命令：

**CUDA 10.1**：
```bash
pip install torch==1.7.1+cu101 torchvision==0.8.2+cu101 -f https://download.pytorch.org/whl/torch_stable.html
```

**CUDA 10.2**：
```bash
pip install torch==1.7.1+cu102 torchvision==0.8.2+cu102 -f https://download.pytorch.org/whl/torch_stable.html
```

**CUDA 11.0**：
```bash
pip install torch==1.7.1+cu110 torchvision==0.8.2+cu110 -f https://download.pytorch.org/whl/torch_stable.html
```

**CUDA 11.1+**：
```bash
pip install torch==1.8.1+cu111 torchvision==0.9.1+cu111 -f https://download.pytorch.org/whl/torch_stable.html
```

**仅CPU版本**：
```bash
pip install torch==1.7.1+cpu torchvision==0.8.2+cpu -f https://download.pytorch.org/whl/torch_stable.html
```

## 三、特殊依赖安装说明

### 1. 点云可视化组件编译

项目中的visualizer目录需要编译C++扩展：

**Windows环境**：
```powershell
cd visualizer
# 需要Visual Studio环境
# 手动编译render_balls_so.cpp为DLL文件
```

**Linux/Mac环境**：
```bash
cd visualizer
chmod +x build.sh
./build.sh
```

### 2. Open3D安装选项

Open3D提供多种安装方式：

```bash
# 标准安装
pip install open3d>=0.15.0

# 带CUDA支持的安装（如果需要）
pip install open3d-cuda11.3  # 根据CUDA版本选择
```

### 3. Mayavi安装注意事项

Mayavi依赖较多，建议使用conda安装：

```bash
conda install -c conda-forge mayavi
```

## 四、常见问题解决方案

### 1. CUDA相关问题

- **问题**：安装PyTorch后无法使用GPU
  **解决方案**：确认CUDA版本与PyTorch版本匹配，可通过以下代码测试：
  ```python
  import torch
  print(torch.cuda.is_available())
  ```

- **问题**：CUDA内存不足
  **解决方案**：减小batch_size，或使用梯度累积，或调用`torch.cuda.empty_cache()`释放缓存

### 2. 点云可视化问题

- **问题**：无法加载render_balls_so库
  **解决方案**：重新编译C++扩展，确保编译器兼容

- **问题**：OpenCV可视化窗口崩溃
  **解决方案**：尝试使用`cv2.waitKey(1)`或切换到matplotlib后端

### 3. 依赖冲突问题

- **问题**：安装新依赖后原有功能失效
  **解决方案**：使用`pip list`检查版本，或创建新的虚拟环境重新安装

- **问题**：numpy版本冲突
  **解决方案**：安装固定版本：`pip install numpy==1.19.5`

## 五、安装验证

创建`check_installation.py`验证安装：

```python
# 基础依赖检查
try:
    import torch
    import torchvision
    import numpy
    import cv2
    import tqdm
    import plyfile
    print("基础依赖安装成功!")
except ImportError as e:
    print(f"基础依赖安装失败: {e}")

# CUDA检查
if torch.cuda.is_available():
    print(f"CUDA可用，版本: {torch.version.cuda}")
    print(f"GPU设备数: {torch.cuda.device_count()}")
else:
    print("CUDA不可用，使用CPU模式")

# 点云读取测试
try:
    from plyfile import PlyData
    print("点云文件读取测试通过")
except Exception as e:
    print(f"点云文件读取测试失败: {e}")
```

运行测试：
```bash
python check_installation.py
```

## 六、性能优化建议

### 1. 依赖版本选择

- 对于生产环境，固定所有依赖版本，避免自动更新
- 对于研究环境，可以使用较新的版本获得更好性能

### 2. CUDA优化

- 确保安装cuDNN库以加速卷积操作
- 使用混合精度训练：`torch.cuda.amp`

### 3. 内存优化

- 使用`pip install numpy==1.19.5`避免内存泄漏
- 定期调用`torch.cuda.empty_cache()`释放GPU内存

## 七、其他注意事项

1. **权限问题**：在Linux/Mac上使用`sudo`时注意权限问题
2. **网络问题**：可以使用国内镜像源加速安装：
   ```bash
   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```
3. **Python版本**：建议使用Python 3.8，兼容性最佳
4. **升级策略**：先备份环境，再升级单个库，测试后再升级其他库
5. **卸载问题**：使用`pip uninstall -y 库名`彻底卸载库及其依赖