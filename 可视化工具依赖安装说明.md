# 可视化工具依赖安装说明

## 需要新安装的依赖

运行 `visualize_predicted_pointcloud.py` 脚本需要以下新安装的依赖：

### 1. Open3D

Open3D 是一个开源的3D数据处理库，在我们的脚本中作为备选可视化方案使用。它不在项目原有的 `requirements.txt` 文件中，需要额外安装。

#### Windows 系统安装命令：
```bash
pip install open3d
```

#### Linux 系统安装命令：
```bash
pip install open3d
```

#### 验证安装：
```bash
python -c "import open3d as o3d; print('Open3D 版本:', o3d.__version__)"
```

## 项目已有的依赖

以下依赖已经在项目的 `requirements.txt` 文件中列出：

- torch>=1.6.0
- torchvision>=0.7.0
- numpy>=1.19.0
- tqdm>=4.50.0
- opencv-python>=4.4.0
- plyfile>=0.7.4

## 安装所有依赖的方法

### 1. 安装原有依赖
```bash
pip install -r requirements.txt
```

### 2. 安装新增依赖
```bash
pip install open3d
```

## 注意事项

1. **Open3D 安装问题**：
   - 如果遇到安装问题，可以尝试使用 conda 安装：
     ```bash
     conda install -c open3d-admin open3d
     ```
   - 或者从源代码编译安装（适用于高级用户）

2. **可视化渲染依赖**：
   - `show3d_balls.py` 依赖于编译好的 `render_balls_so.so`（Linux）或 `render_balls_so.dll`（Windows）
   - 如果缺少此文件，请运行 `visualizer` 目录下的编译脚本：
     ```bash
     cd visualizer
     # Linux 系统
     bash build.sh
     # Windows 系统可能需要手动编译
     ```

3. **备选方案**：
   - 如果 Open3D 安装失败，可以使用 `--use_open3d=False` 参数强制使用原生的 `show3d_balls` 可视化工具

4. **CUDA 支持**：
   - 如果需要 GPU 加速，确保安装的 PyTorch 版本支持 CUDA

## 完整安装命令（一键安装）

```bash
pip install -r requirements.txt && pip install open3d
```

安装完成后，您就可以正常运行点云分类结果可视化工具了！